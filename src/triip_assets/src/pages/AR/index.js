import React, { Suspense, useEffect, useState } from "react";
import { Outlet } from "react-router";
const AR = () => {
  return (
    <>
      <Outlet />
    </>
  );
};
export default AR;
